# 区域管理后台-概要设计v1.0

------

# 一、项目背景

目前销售区域管理存在几个问题：

- 区域划分标准不统一
- 各系统之间数据不一致
- 缺少标准化的查询能力

需要建设一个销售区域管理中台系统，作为统一的组织与区域数据源，为上游系统提供标准 API 服务。

------

# 二、建设目标

1. 建立统一的销售组织架构（大区 / 办事处 / 片区）
2. 与国家行政区划（省/市/区/街道）建立映射关系
3. 提供标准查询 API，支持多系统调用
4. 提供统一鉴权与安全机制
5. 支持高扩展性，未来可扩展渠道、人员等能力

------

# 三、系统整体架构



------

# 四、核心数据模型设计

 [01-数据库设计v1.0.md](01-数据库设计.md)

## 4.1 数据模型关系

```
行政区（area）
    ↓
行政区映射表（area_relation）
    ↓
组织架构（organization）
```

------

## 4.2 组织模型说明

统一使用 organization 表：

| 类型 | 说明   |
| ---- | ------ |
| 1    | 大区   |
| 2    | 办事处 |
| 3    | 片区   |

通过以下字段实现树结构：

- parent_id：父节点
- path：路径（避免递归查询）
- level：层级

示例：

```
华南大区（1）
  └ 深圳办事处（2）
       └ 南山片区（3）
```

------

## 4.3 行政区模型

基于标准四级结构：

- 省 - 市-区/县 - 街道/乡镇

通过 pid（父级）和 path（完整路径）实现树形结构。

------

## 4.4 映射关系设计

area_relation 表的设计：

- 一个片区对应多个街道
- 一个街道只能归属一个片区（建议通过业务约束保证）

------

# 五、功能设计

------

## 5.1 登录模块

功能：

- 用户登录：使用用户名和密码方式登录

------

## 5.2 大区管理

功能列表：

- 新建大区
- 编辑大区
- 删除（软删除）
- 启用/禁用

关键字段：

- name
- type = 1
- parent_id = 0

------

## 5.3 办事处管理

功能点：

- 新建办事处（必须选择所属大区）
- 编辑
- 删除

------

## 5.4 片区管理

功能点：

- 新建片区（必须选择所属办事处）
- 编辑
- 删除

------

## 5.5 行政区绑定

功能点：

- 给片区绑定街道
- 支持批量选择
- 支持树形选择（省市区街道）

------

## 5.6 行政区管理

功能点：

- 行政区数据导入（支持从民政部标准数据批量导入）
- 行政区树形浏览（支持按层级展开）
- 行政区详情查看（含完整上级路径）
- 手动新增行政区（处理特殊情况，如功能区、开发区等非标准区划）
- 编辑行政区信息（名称、经纬度、简称等）
- 启用/禁用行政区（保留历史数据，禁用后不可绑定）
- 删除行政区（无子节点且无绑定关系时可删除）

关键字段：

- code（行政区划代码，唯一标识）
- name（行政区名称）
- level（层级：1-省 2-市 3-区/县 4-街道/乡镇）
- parent_code（上级区划代码）
- path（完整路径，如 "广东省/深圳市/南山区/南头街道"）
- lng / lat（经纬度坐标）
- status（状态：1-启用 0-禁用）

业务规则：

1. 行政区划代码全局唯一，不允许修改
2. 新增行政区必须指定上级行政区（省级别除外）
3. 禁用行政区时，其下所有子级行政区联动禁用
4. 有子节点或已绑定片区/办事处的行政区不可删除
5. 数据导入时自动检测增量变更（新增、更名、合并、撤销）

------

# 六、核心业务规则

1. 组织层级固定为 3 级：大区 → 办事处 → 片区
2. 大区、办事处、片区名称在各自层级内必须唯一
3. 任意一个省、市、县、区仅归属一个大区，即仅归属一个办事处；任何一个乡镇/街道仅归属一个片区
4. 大区/办事处关联行政区域到区县级别，片区关联行政区域到乡镇/街道级别
5. 大区/办事处/片区的删除规则：
   - 有子节点不可删除
   - 有绑定关系不可删除

------

# 七、对外 API 设计

------

## 7.1 统一规范

- 协议：HTTP/HTTPS
- 格式：JSON
- 鉴权：签名 + 时间戳 + appKey + nonce
- 字符编码：UTF-8

## 7.2 统一响应格式

### 响应结构

所有接口返回统一的 JSON 格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

### 响应字段说明

| 字段      | 类型   | 说明                      |
| --------- | ------ | ------------------------- |
| code      | int    | 状态码，0 表示成功         |
| message   | string | 响应消息，成功时返回 success |
| data      | object/array | 业务数据，失败时可为 null |
| timestamp | long   | 服务器响应时间戳          |
| traceId   | string | 请求追踪 ID，用于问题排查  |

### 错误码定义

| 错误码  | 说明             |
| ------- | ---------------- |
| 0       | 成功             |
| 400     | 请求参数错误     |
| 401     | 签名验证失败     |
| 403     | 无权限访问       |
| 404     | 资源不存在       |
| 500     | 服务器内部错误   |
| 503     | 服务暂时不可用   |

### 错误响应示例

```json
{
  "code": 401,
  "message": "签名验证失败",
  "data": null,
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

## 7.3 签名机制

### 签名算法说明

采用 MD5 签名算法，确保请求的完整性和不可篡改性。

签名公式：

```
sign = MD5(appKey + timestamp + nonce + body + secret)
```

参数说明：

| 参数      | 说明                           | 要求                |
| --------- | ------------------------------ | ------------------- |
| appKey    | 调用方标识                     | 由平台分配          |
| timestamp | Unix 时间戳（毫秒）             | 与服务器时间差≤5 分钟 |
| nonce     | 随机字符串（UUID 或 16 位随机数） | 防止重放攻击        |
| body      | 请求体原始 JSON 字符串          | GET 请求为空字符串   |
| secret    | 调用方密钥                     | 由平台分配          |

签名生成步骤：

1. 将 appKey、timestamp、nonce、body、secret 按顺序拼接
2. 对拼接后的字符串进行 MD5 加密
3. 将 MD5 结果转为大写（32 位）

### Java 示例代码

```java
import org.apache.commons.codec.digest.DigestUtils;
import java.util.UUID;

public class ApiSignatureUtil {
    
    private static final String APP_KEY = "your_app_key";
    private static final String SECRET = "your_secret";
    
    /**
     * 生成 API 请求签名
     * @param body 请求体 JSON 字符串，GET 请求传空字符串
     * @return 签名值（大写 32 位）
     */
    public static String generateSignature(String body) {
        long timestamp = System.currentTimeMillis();
        String nonce = UUID.randomUUID().toString().replace("-", "");
        
        // 拼接签名字符串
        String signStr = APP_KEY + timestamp + nonce + body + SECRET;
        
        // MD5 加密并转大写
        return DigestUtils.md5Hex(signStr).toUpperCase();
    }
    
    /**
     * 生成带签名信息的请求头
     * @param body 请求体 JSON 字符串
     * @return 请求头 Map
     */
    public static Map<String, String> generateHeaders(String body) {
        Map<String, String> headers = new HashMap<>();
        headers.put("appKey", APP_KEY);
        headers.put("timestamp", String.valueOf(System.currentTimeMillis()));
        headers.put("nonce", UUID.randomUUID().toString().replace("-", ""));
        headers.put("sign", generateSignature(body));
        return headers;
    }
    
    /**
     * 示例：发起 GET 请求
     */
    public static String get(String url) throws Exception {
        String body = ""; // GET 请求 body 为空
        Map<String, String> headers = generateHeaders(body);
        
        // 使用 HttpClient 或 OkHttp 发起请求
        // ... HTTP 请求代码
        return null;
    }
    
    /**
     * 示例：发起 POST 请求
     */
    public static String post(String url, String jsonBody) throws Exception {
        Map<String, String> headers = generateHeaders(jsonBody);
        headers.put("Content-Type", "application/json");
        
        // 使用 HttpClient 或 OkHttp 发起请求
        // ... HTTP 请求代码
        return null;
    }
}
```

### Maven 依赖

```xml
<dependency>
    <groupId>commons-codec</groupId>
    <artifactId>commons-codec</artifactId>
    <version>1.15</version>
</dependency>
```

### 请求头规范

| 参数        | 必填 | 说明           | 示例                  |
| ----------- | ---- | -------------- | --------------------- |
| appKey      | 是   | 调用方标识     | `JZ_SALES_001`        |
| timestamp   | 是   | Unix 时间戳    | `1713580800000`       |
| nonce       | 是   | 随机字符串     | `a1b2c3d4e5f6g7h8`    |
| sign        | 是   | 签名值         | `E94A2B5C8D1F3A7B...` |
| Content-Type | POST 必填 | 内容类型   | `application/json`    |

------


## 7.4 API 列表

接口设计规范：
- 所有接口仅使用 GET 和 POST 两种方式
- 查询类接口使用 GET，参数通过查询字符串传递
- 新增/修改/删除类接口使用 POST，参数通过请求体传递

------

### 认证相关接口

------

#### 用户登录

接口描述：用户通过用户名密码登录，获取访问 Token

请求：

```
POST /api/auth/login
```

请求头：

| 参数        | 必填 | 说明         |
| ----------- | ---- | ------------ |
| Content-Type | 是   | application/json |

请求参数：

| 参数     | 类型   | 必填 | 说明           | 示例            |
| -------- | ------ | ---- | -------------- | --------------- |
| username | string | 是   | 用户名         | `zhangsan`      |
| password | string | 是   | 密码（MD5 加密） | `e10adc3949ba...` |

请求示例：

```json
{
  "username": "zhangsan",
  "password": "e10adc3949ba59abbe56e057f20f883e"
}
```

返回参数：

| 参数      | 类型   | 说明         |
| --------- | ------ | ------------ |
| token     | string | 访问令牌     |
| expiresIn | long   | 过期时间 (秒)  |
| userInfo  | object | 用户信息     |

userInfo 对象结构：

| 字段   | 类型   | 说明     |
| ------ | ------ | -------- |
| userId | long   | 用户 ID  |
| username | string | 用户名   |
| realName | string | 真实姓名 |
| roles  | array  | 角色列表 |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expiresIn": 7200,
    "userInfo": {
      "userId": 1001,
      "username": "zhangsan",
      "realName": "张三",
      "roles": ["ADMIN", "REGION_MANAGER"]
    }
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```


------

### 大区管理接口

------

#### 查询所有大区

接口描述：获取所有大区列表，支持按状态筛选

请求：

```
GET /api/region/list
```

请求参数：

| 参数   | 类型   | 必填 | 说明                    | 示例  |
| ------ | ------ | ---- | ----------------------- | ----- |
| status | int    | 否   | 状态：1-启用，0-禁用    | `1`   |
| page   | int    | 否   | 页码，默认 1            | `1`   |
| size   | int    | 否   | 每页数量，默认 10       | `10`  |

返回参数：

| 参数       | 类型   | 说明       |
| ---------- | ------ | ---------- |
| list       | array  | 大区列表   |
| pagination | object | 分页信息   |

list 数组元素结构：

| 字段       | 类型   | 说明     |
| ---------- | ------ | -------- |
| id         | long   | 大区 ID  |
| name       | string | 大区名称 |
| status     | int    | 状态     |
| createTime | string | 创建时间 |

pagination 对象结构：

| 字段    | 类型 | 说明     |
| ------- | ---- | -------- |
| page    | int  | 当前页码 |
| size    | int  | 每页数量 |
| total   | long | 总记录数 |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "name": "华南大区",
        "status": 1,
        "createTime": "2024-01-01 10:00:00"
      },
      {
        "id": 2,
        "name": "华东大区",
        "status": 1,
        "createTime": "2024-01-02 10:00:00"
      }
    ],
    "pagination": {
      "page": 1,
      "size": 10,
      "total": 2
    }
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 创建大区

接口描述：新建一个大区

请求：

```
POST /api/region/create
```

请求参数：

| 参数   | 类型   | 必填 | 说明     | 示例       |
| ------ | ------ | ---- | -------- | ---------- |
| name   | string | 是   | 大区名称 | `华南大区` |
| remark | string | 否   | 备注     | `负责华南区域` |

返回参数：返回创建成功的大区信息

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 3,
    "name": "华北大区",
    "status": 1,
    "createTime": "2024-04-20 10:00:00"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 更新大区

接口描述：修改大区信息

请求：

```
POST /api/region/update
```

请求参数：

| 参数   | 类型   | 必填 | 说明     |
| ------ | ------ | ---- | -------- |
| id     | long   | 是   | 大区 ID  |
| name   | string | 是   | 大区名称 |
| status | int    | 否   | 状态     |
| remark | string | 否   | 备注     |

返回参数：返回更新后的大区信息

------

#### 删除大区

接口描述：删除大区（软删除，有子节点或绑定关系时不可删除）

请求：

```
POST /api/region/delete
```

请求参数：

| 参数 | 类型 | 必填 | 说明   |
| ---- | ---- | ---- | ------ |
| id   | long | 是   | 大区 ID |

返回参数：

| 参数    | 类型    | 说明               |
| ------- | ------- | ------------------ |
| success | boolean | 是否删除成功       |
| message | string  | 提示信息           |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "success": true,
    "message": "删除成功"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

### 办事处管理接口

------

#### 查询大区下办事处

接口描述：获取指定大区下的所有办事处列表

请求：

```
GET /api/office/list
```

请求参数：

| 参数     | 类型 | 必填 | 说明                 |
| -------- | ---- | ---- | -------------------- |
| regionId | long | 否   | 大区 ID，不传返回全部 |
| status   | int  | 否   | 状态：1-启用，0-禁用 |
| page     | int  | 否   | 页码，默认 1         |
| size     | int  | 否   | 每页数量，默认 10    |

返回参数：

| 字段       | 类型   | 说明       |
| ---------- | ------ | ---------- |
| id         | long   | 办事处 ID  |
| name       | string | 办事处名称 |
| regionId   | long   | 所属大区 ID |
| regionName | string | 所属大区名称 |
| status     | int    | 状态       |
| createTime | string | 创建时间   |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 101,
        "name": "深圳办事处",
        "regionId": 1,
        "regionName": "华南大区",
        "status": 1,
        "createTime": "2024-01-10 10:00:00"
      },
      {
        "id": 102,
        "name": "广州办事处",
        "regionId": 1,
        "regionName": "华南大区",
        "status": 1,
        "createTime": "2024-01-11 10:00:00"
      }
    ],
    "pagination": {
      "page": 1,
      "size": 10,
      "total": 2
    }
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 创建办事处

接口描述：在大区下新建办事处

请求：

```
POST /api/office/create
```

请求参数：

| 参数     | 类型   | 必填 | 说明       |
| -------- | ------ | ---- | ---------- |
| name     | string | 是   | 办事处名称 |
| regionId | long   | 是   | 所属大区 ID |
| remark   | string | 否   | 备注       |

返回参数：返回创建的办事处信息

------

#### 更新办事处

接口描述：修改办事处信息

请求：

```
POST /api/office/update
```

请求参数：

| 参数     | 类型   | 必填 | 说明       |
| -------- | ------ | ---- | ---------- |
| id       | long   | 是   | 办事处 ID  |
| name     | string | 是   | 办事处名称 |
| status   | int    | 否   | 状态       |
| remark   | string | 否   | 备注       |

返回参数：返回更新后的办事处信息

------

#### 删除办事处

接口描述：删除办事处（有子节点或绑定关系时不可删除）

请求：

```
POST /api/office/delete
```

请求参数：

| 参数 | 类型 | 必填 | 说明     |
| ---- | ---- | ---- | -------- |
| id   | long | 是   | 办事处 ID |

返回参数：同删除大区接口

------

### 片区管理接口

------

#### 查询办事处下片区

接口描述：获取指定办事处下的所有片区列表

请求：

```
GET /api/area/list
```

请求参数：

| 参数     | 类型 | 必填 | 说明                   |
| -------- | ---- | ---- | ---------------------- |
| officeId | long | 否   | 办事处 ID，不传返回全部 |
| page     | int  | 否   | 页码，默认 1           |
| size     | int  | 否   | 每页数量，默认 10      |

返回参数：

| 字段       | 类型   | 说明       |
| ---------- | ------ | ---------- |
| id         | long   | 片区 ID    |
| name       | string | 片区名称   |
| officeId   | long   | 所属办事处 ID |
| officeName | string | 所属办事处名称 |
| status     | int    | 状态       |
| createTime | string | 创建时间   |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "南山片区",
        "officeId": 101,
        "officeName": "深圳办事处",
        "status": 1,
        "createTime": "2024-02-01 10:00:00"
      },
      {
        "id": 1002,
        "name": "福田片区",
        "officeId": 101,
        "officeName": "深圳办事处",
        "status": 1,
        "createTime": "2024-02-02 10:00:00"
      }
    ],
    "pagination": {
      "page": 1,
      "size": 10,
      "total": 2
    }
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 创建片区

接口描述：在办事处下新建片区

请求：

```
POST /api/area/create
```

请求参数：

| 参数     | 类型   | 必填 | 说明       |
| -------- | ------ | ---- | ---------- |
| name     | string | 是   | 片区名称   |
| officeId | long   | 是   | 所属办事处 ID |
| remark   | string | 否   | 备注       |

返回参数：返回创建的片区信息

------

#### 更新片区

接口描述：修改片区信息

请求：

```
POST /api/area/update
```

请求参数：

| 参数     | 类型   | 必填 | 说明     |
| -------- | ------ | ---- | -------- |
| id       | long   | 是   | 片区 ID  |
| name     | string | 是   | 片区名称 |
| status   | int    | 否   | 状态     |
| remark   | string | 否   | 备注     |

返回参数：返回更新后的片区信息

------

#### 删除片区

接口描述：删除片区（有绑定街道时不可删除）

请求：

```
POST /api/area/delete
```

请求参数：

| 参数 | 类型 | 必填 | 说明   |
| ---- | ---- | ---- | ------ |
| id   | long | 是   | 片区 ID |

返回参数：同删除大区接口

------

### 街道/行政区管理接口

------

#### 查询片区管辖街道

接口描述：获取指定片区管辖的所有街道列表

请求：

```
GET /api/street/list
```

请求参数：

| 参数   | 类型 | 必填 | 说明                   |
| ------ | ---- | ---- | ---------------------- |
| areaId | long | 否   | 片区 ID，不传返回全部   |

返回参数：

| 字段     | 类型   | 说明         |
| -------- | ------ | ------------ |
| id       | long   | 行政区 ID    |
| code     | string | 行政区划代码 |
| name     | string | 行政区名称   |
| level    | int    | 行政级别     |
| parentId | long   | 父级 ID      |
| fullName | string | 完整路径名   |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 10001,
        "code": "440305001",
        "name": "南头街道",
        "level": 4,
        "parentId": 440305,
        "fullName": "广东省深圳市南山区南头街道"
      },
      {
        "id": 10002,
        "code": "440305002",
        "name": "桃源街道",
        "level": 4,
        "parentId": 440305,
        "fullName": "广东省深圳市南山区桃源街道"
      }
    ]
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 为片区绑定街道

接口描述：将行政区（街道）绑定到片区

请求：

```
POST /api/area/bind-streets
```

请求参数：

| 参数      | 类型  | 必填 | 说明         |
| --------- | ----- | ---- | ------------ |
| areaId    | long  | 是   | 片区 ID      |
| streetIds | array | 是   | 街道 ID 列表  |

请求示例：

```json
{
  "areaId": 1001,
  "streetIds": [10001, 10002, 10003]
}
```

返回参数：

| 参数       | 类型    | 说明     |
| ---------- | ------- | -------- |
| success    | boolean | 是否成功 |
| boundCount | int     | 绑定数量 |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "success": true,
    "boundCount": 3
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 解除片区街道绑定

接口描述：解除片区与街道的绑定关系

请求：

```
POST /api/area/unbind-street
```

请求参数：

| 参数     | 类型 | 必填 | 说明     |
| -------- | ---- | ---- | -------- |
| areaId   | long | 是   | 片区 ID  |
| streetId | long | 是   | 街道 ID  |

返回参数：

| 参数    | 类型    | 说明     |
| ------- | ------- | -------- |
| success | boolean | 是否成功 |

------

### 行政区管理接口

------

#### 行政区树形查询

接口描述：获取完整的行政区划树（省 → 市 → 区/县 → 街道/乡镇），支持指定起始节点和层级深度

请求：

```
GET /api/admin-area/tree
```

请求参数：

| 参数      | 类型 | 必填 | 说明                                         |
| --------- | ---- | ---- | -------------------------------------------- |
| parentCode | string | 否   | 上级区划代码，不传从省级开始                 |
| depth     | int  | 否   | 向下展开层级深度，默认 1                     |
| status    | int  | 否   | 状态筛选：1-启用 0-禁用 不传返回全部         |

返回参数：

| 字段       | 类型   | 说明             |
| ---------- | ------ | ---------------- |
| code       | string | 行政区划代码     |
| name       | string | 行政区名称       |
| level      | int    | 层级             |
| status     | int    | 状态             |
| boundOrg   | object | 绑定组织信息（如有） |
| children   | array  | 下级行政区列表   |

boundOrg 对象结构：

| 字段         | 类型   | 说明           |
| ------------ | ------ | -------------- |
| orgType      | string | 组织类型：district/office/region |
| orgId        | long   | 组织 ID        |
| orgName      | string | 组织名称       |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "code": "440000",
      "name": "广东省",
      "level": 1,
      "status": 1,
      "children": [
        {
          "code": "440300",
          "name": "深圳市",
          "level": 2,
          "status": 1,
          "children": [
            {
              "code": "440305",
              "name": "南山区",
              "level": 3,
              "status": 1,
              "children": [
                {
                  "code": "440305001",
                  "name": "南头街道",
                  "level": 4,
                  "status": 1,
                  "boundOrg": {
                    "orgType": "district",
                    "orgId": 1001,
                    "orgName": "南山片区"
                  },
                  "children": []
                }
              ]
            }
          ]
        }
      ]
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 行政区详情查询

接口描述：根据行政区划代码查询详细信息（含完整上级路径和绑定关系）

请求：

```
GET /api/admin-area/detail
```

请求参数：

| 参数 | 类型   | 必填 | 说明           |
| ---- | ------ | ---- | -------------- |
| code | string | 是   | 行政区划代码   |

返回参数：

| 字段         | 类型   | 说明               |
| ------------ | ------ | ------------------ |
| code         | string | 行政区划代码       |
| name         | string | 行政区名称         |
| shortName    | string | 简称               |
| level        | int    | 层级               |
| parentCode   | string | 上级区划代码       |
| parentName   | string | 上级区划名称       |
| path         | string | 完整路径           |
| lng          | double | 经度               |
| lat          | double | 纬度               |
| status       | int    | 状态               |
| boundOrgType | string | 绑定组织类型       |
| boundOrgId   | long   | 绑定组织 ID        |
| boundOrgName | string | 绑定组织名称       |
| createTime   | string | 创建时间           |
| updateTime   | string | 更新时间           |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "code": "440305001",
    "name": "南头街道",
    "shortName": "南头",
    "level": 4,
    "parentCode": "440305",
    "parentName": "南山区",
    "path": "广东省/深圳市/南山区/南头街道",
    "lng": 113.942695,
    "lat": 22.532848,
    "status": 1,
    "boundOrgType": "district",
    "boundOrgId": 1001,
    "boundOrgName": "南山片区",
    "createTime": "2024-01-01 00:00:00",
    "updateTime": "2024-01-01 00:00:00"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 新增行政区

接口描述：手动新增一个行政区（用于处理功能区、开发区等非标准区划）

请求：

```
POST /api/admin-area/create
```

请求参数：

| 参数      | 类型   | 必填 | 说明                                         |
| --------- | ------ | ---- | -------------------------------------------- |
| code      | string | 是   | 行政区划代码（全局唯一）                     |
| name      | string | 是   | 行政区名称                                   |
| shortName | string | 否   | 简称                                         |
| level     | int    | 是   | 层级：1-省 2-市 3-区/县 4-街道/乡镇          |
| parentCode| string | 否   | 上级区划代码（省级可不传）                   |
| lng       | double | 否   | 经度                                         |
| lat       | double | 否   | 纬度                                         |
| remark    | string | 否   | 备注                                         |

返回参数：返回新增的行政区信息

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "code": "440399",
    "name": "深汕特别合作区",
    "shortName": "深汕",
    "level": 3,
    "parentCode": "440300",
    "status": 1,
    "createTime": "2024-04-20 10:00:00"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 更新行政区

接口描述：修改行政区信息（区划代码不可修改）

请求：

```
POST /api/admin-area/update
```

请求参数：

| 参数      | 类型   | 必填 | 说明                   |
| --------- | ------ | ---- | ---------------------- |
| code      | string | 是   | 行政区划代码           |
| name      | string | 否   | 行政区名称             |
| shortName | string | 否   | 简称                   |
| lng       | double | 否   | 经度                   |
| lat       | double | 否   | 纬度                   |
| remark    | string | 否   | 备注                   |

返回参数：返回更新后的行政区信息

------

#### 删除行政区

接口描述：删除行政区（有子节点或已绑定组织时不可删除）

请求：

```
POST /api/admin-area/delete
```

请求参数：

| 参数 | 类型   | 必填 | 说明           |
| ---- | ------ | ---- | -------------- |
| code | string | 是   | 行政区划代码   |

返回参数：

| 参数    | 类型    | 说明               |
| ------- | ------- | ------------------ |
| success | boolean | 是否删除成功       |
| message | string  | 提示信息           |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "success": true,
    "message": "删除成功"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 启用/禁用行政区

接口描述：启用或禁用行政区（禁用时子级联动禁用）

请求：

```
POST /api/admin-area/status
```

请求参数：

| 参数   | 类型   | 必填 | 说明                           |
| ------ | ------ | ---- | ------------------------------ |
| code   | string | 是   | 行政区划代码                   |
| status | int    | 是   | 状态：1-启用 0-禁用            |

返回参数：

| 参数           | 类型    | 说明                     |
| -------------- | ------- | ------------------------ |
| success        | boolean | 是否成功                 |
| affectedCount  | int     | 联动影响的子级行政区数量 |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "success": true,
    "affectedCount": 12
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 行政区数据导入

接口描述：从外部数据源（民政部标准行政区划数据）批量导入或同步行政区数据

请求：

```
POST /api/admin-area/import
```

请求参数：

| 参数       | 类型   | 必填 | 说明                                         |
| ---------- | ------ | ---- | -------------------------------------------- |
| sourceType | string | 是   | 数据来源：civil_affairs-民政部 standard-国标  |
| year       | string | 否   | 数据年份，不传使用最新                       |
| mode       | string | 否   | 导入模式：full-全量覆盖 incremental-增量更新（默认） |
| dryRun     | boolean| 否   | 是否预演（仅输出变更预览，不实际写入）       |

返回参数：

| 参数        | 类型    | 说明                     |
| ----------- | ------- | ------------------------ |
| success     | boolean | 是否成功                 |
| addedCount  | int     | 新增数量                 |
| updatedCount| int     | 更新数量                 |
| deletedCount| int     | 删除数量（仅全量模式）   |
| skippedCount| int     | 跳过数量                 |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "success": true,
    "addedCount": 15,
    "updatedCount": 3,
    "deletedCount": 0,
    "skippedCount": 2
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 行政区导入预览

接口描述：预览行政区数据导入的变更内容（实际写入前）

请求：

```
POST /api/admin-area/import-preview
```

请求参数：同数据导入接口

返回参数：

| 参数        | 类型  | 说明             |
| ----------- | ----- | ---------------- |
| addedList   | array | 将新增的行政区   |
| updatedList | array | 将更新的行政区   |
| deletedList | array | 将删除的行政区   |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "addedList": [
      { "code": "440399", "name": "深汕特别合作区", "level": 3 }
    ],
    "updatedList": [
      { "code": "440305", "oldName": "南山区", "newName": "前海南山区" }
    ],
    "deletedList": []
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 已禁用行政区查询

接口描述：查询所有已禁用的行政区列表

请求：

```
GET /api/admin-area/disabled
```

请求参数：

| 参数   | 类型 | 必填 | 说明               |
| ------ | ---- | ---- | ------------------ |
| level  | int  | 否   | 层级筛选           |
| page   | int  | 否   | 页码，默认 1       |
| size   | int  | 否   | 每页数量，默认 10  |

返回参数：与列表查询接口一致

------

### 查询类接口

------

#### 查询行政区所属组织

接口描述：根据行政区 ID 查询其所属的组织（片区/办事处/大区）

请求：

```
GET /api/geo/org
```

请求参数：

| 参数   | 类型 | 必填 | 说明       |
| ------ | ---- | ---- | ---------- |
| areaId | long | 是   | 行政区 ID  |

返回参数：

| 字段       | 类型   | 说明       |
| ---------- | ------ | ---------- |
| areaId     | long   | 行政区 ID  |
| areaName   | string | 行政区名称 |
| orgId      | long   | 片区 ID    |
| orgName    | string | 片区名称   |
| officeId   | long   | 办事处 ID  |
| officeName | string | 办事处名称 |
| regionId   | long   | 大区 ID    |
| regionName | string | 大区名称   |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "areaId": 10001,
    "areaName": "南头街道",
    "orgId": 1001,
    "orgName": "南山片区",
    "officeId": 101,
    "officeName": "深圳办事处",
    "regionId": 1,
    "regionName": "华南大区"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 查询片区所属上级

接口描述：查询片区的直接上级（办事处）及所属大区

请求：

```
GET /api/area/parent
```

请求参数：

| 参数   | 类型 | 必填 | 说明   |
| ------ | ---- | ---- | ------ |
| areaId | long | 是   | 片区 ID |

返回参数：

| 字段       | 类型   | 说明       |
| ---------- | ------ | ---------- |
| areaId     | long   | 片区 ID    |
| areaName   | string | 片区名称   |
| officeId   | long   | 办事处 ID  |
| officeName | string | 办事处名称 |
| regionId   | long   | 大区 ID    |
| regionName | string | 大区名称   |

------

#### 查询办事处所属大区

接口描述：查询办事处的所属大区

请求：

```
GET /api/office/region
```

请求参数：

| 参数     | 类型 | 必填 | 说明     |
| -------- | ---- | ---- | -------- |
| officeId | long | 是   | 办事处 ID |

返回参数：

| 字段       | 类型   | 说明       |
| ---------- | ------ | ---------- |
| officeId   | long   | 办事处 ID  |
| officeName | string | 办事处名称 |
| regionId   | long   | 大区 ID    |
| regionName | string | 大区名称   |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "officeId": 101,
    "officeName": "深圳办事处",
    "regionId": 1,
    "regionName": "华南大区"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 组织树查询

接口描述：获取完整的组织架构树（大区 → 办事处 → 片区）

请求：

```
GET /api/org/tree
```

请求参数：

| 参数     | 类型 | 必填 | 说明                   |
| -------- | ---- | ---- | ---------------------- |
| regionId | long | 否   | 大区 ID，不传返回全部   |
| status   | int  | 否   | 状态筛选               |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "华南大区",
      "type": 1,
      "children": [
        {
          "id": 101,
          "name": "深圳办事处",
          "type": 2,
          "children": [
            {
              "id": 1001,
              "name": "南山片区",
              "type": 3,
              "children": []
            }
          ]
        }
      ]
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```


#### 省列表查询

接口描述：获取所有省份列表

请求：

```
GET /api/area/provinces
```

请求参数：无

返回参数：

| 字段      | 类型   | 说明         |
| --------- | ------ | ------------ |
| id        | long   | 省份 ID      |
| name      | string | 省份名称     |
| shortName | string | 简称         |
| Adcode  | string | 行政区划代码     |
| lng       | double | 经度         |
| lat       | double | 纬度         |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 100000,
      "name": "广东省",
      "shortName": "广东",
      "Adcode": "440000",
      "lng": 113.280637,
      "lat": 23.125178
    },
    {
      "id": 110000,
      "name": "北京市",
      "shortName": "北京",
      "Adcode": "110000",
      "lng": 116.407526,
      "lat": 39.90403
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 市列表查询

接口描述：根据省份 ID 查询下属所有城市

请求：

```
GET /api/area/cities
```

请求参数：

| 参数      | 类型 | 必填 | 说明     |
| --------- | ---- | ---- | -------- |
| provinceId | long | 是   | 省份 ID  |

返回参数：

| 字段      | 类型   | 说明         |
| --------- | ------ | ------------ |
| id        | long   | 城市 ID      |
| name      | string | 城市名称     |
| shortName | string | 简称         |
| Adcode  | string | 行政区划代码     |
| lng       | double | 经度         |
| lat       | double | 纬度         |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 440300,
      "name": "深圳市",
      "shortName": "深圳",
      "Adcode": "440300",
      "lng": 114.057868,
      "lat": 22.543099
    },
    {
      "id": 440100,
      "name": "广州市",
      "shortName": "广州",
      "Adcode": "440100",
      "lng": 113.280637,
      "lat": 23.125178
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 区/县列表查询

接口描述：根据城市 ID 查询下属所有区县

请求：

```
GET /api/area/counties
```

请求参数：

| 参数     | 类型 | 必填 | 说明     |
| -------- | ---- | ---- | -------- |
| cityId   | long | 是   | 城市 ID  |

返回参数：

| 字段      | 类型   | 说明         |
| --------- | ------ | ------------ |
| id        | long   | 区县 ID      |
| name      | string | 区县名称     |
| shortName | string | 简称         |
| level     | int    | 类型：1-市辖区 2-县 3-县级市 |
| Adcode  | string | 行政区划代码     |
| lng       | double | 经度         |
| lat       | double | 纬度         |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 440305,
      "name": "南山区",
      "shortName": "南山",
      "level": 1,
      "Adcode": "440305",
      "lng": 113.928433,
      "lat": 22.489046
    },
    {
      "id": 440306,
      "name": "宝安区",
      "shortName": "宝安",
      "level": 1,
      "Adcode": "440306",
      "lng": 113.884837,
      "lat": 22.652203
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 乡镇/街道列表查询

接口描述：根据区县 ID 查询下属所有乡镇和街道

请求：

```
GET /api/area/townships
```

请求参数：

| 参数     | 类型 | 必填 | 说明       |
| -------- | ---- | ---- | ---------- |
| countyId | long | 是   | 区县 ID    |

返回参数：

| 字段      | 类型   | 说明             |
| --------- | ------ | ---------------- |
| id        | long   | 乡镇/街道 ID     |
| name      | string | 名称             |
| shortName | string | 简称             |
| type      | string | 类型：街道/镇/乡 |
| Adcode  | string | 行政区划代码         |
| lng       | double | 经度             |
| lat       | double | 纬度             |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 440305001,
      "name": "南头街道",
      "shortName": "南头",
      "type": "街道",
      "Adcode": "440305001",
      "lng": 113.942695,
      "lat": 22.532848
    },
    {
      "id": 440305002,
      "name": "南山街道",
      "shortName": "南山",
      "type": "街道",
      "Adcode": "440305002",
      "lng": 113.915856,
      "lat": 22.528993
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```


#### 行政区详情查询

接口描述：根据行政区 ID 查询详细信息（含完整上级路径）

请求：

```
GET /api/area/detail
```

请求参数：

| 参数   | 类型 | 必填 | 说明         |
| ------ | ---- | ---- | ------------ |
| areaId | long | 是   | 行政区 ID    |

返回参数：

| 字段       | 类型   | 说明           |
| ---------- | ------ | -------------- |
| id         | long   | 行政区 ID      |
| name       | string | 名称           |
| fullName   | string | 完整路径名称   |
| level      | int    | 层级           |
| provinceId | long   | 所属省份 ID    |
| provinceName | string | 省份名称   |
| cityId     | long   | 所属城市 ID    |
| cityName   | string | 城市名称       |
| countyId   | long   | 所属区县 ID    |
| countyName | string | 区县名称       |
| townshipId | long   | 所属乡镇/街道 ID |
| townshipName | string | 乡镇/街道名称 |
| lng        | double | 经度           |
| lat        | double | 纬度           |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 440305001,
    "name": "南头街道",
    "fullName": "广东省深圳市南山区南头街道",
    "level": 3,
    "provinceId": 100000,
    "provinceName": "广东省",
    "cityId": 440300,
    "cityName": "深圳市",
    "countyId": 440305,
    "countyName": "南山区",
    "townshipId": 440305001,
    "townshipName": "南头街道",
    "lng": 113.942695,
    "lat": 22.532848
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 行政区树查询

接口描述：根据类型获取行政区树（可指定层级深度）

请求：

```
GET /api/area/tree
```

请求参数：

| 参数    | 类型 | 必填 | 说明                                         |
| ------- | ---- | ---- | -------------------------------------------- |
| type    | int  | 否   | 起始层级：0-省（默认）1-市 2-区县 3-乡镇街道 |
| areaId  | long | 否   | 起始行政区 ID（type 为 0 时不传）            |
| depth   | int  | 否   | 向下查询层级深度，默认 1                     |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 100000,
      "name": "广东省",
      "children": [
        {
          "id": 440300,
          "name": "深圳市",
          "children": [
            {
              "id": 440305,
              "name": "南山区",
              "children": []
            }
          ]
        }
      ]
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```



#### 根据街道行政区 ID 查询所属片区

接口描述：根据街道（乡镇）行政区 ID 查询其所属片区信息，返回结果包含大区、办事处、片区各自的 ID 和名称

请求：

```
GET /api/geo/org/by-township
```

请求参数：

| 参数       | 类型 | 必填 | 说明               |
| ---------- | ---- | ---- | ------------------ |
| townshipId | long | 是   | 街道/乡镇行政区 ID |

返回参数：

| 字段         | 类型   | 说明             |
| ------------ | ------ | ---------------- |
| townshipId   | long   | 街道/乡镇行政区 ID |
| townshipName | string | 街道/乡镇名称    |
| regionId     | long   | 所属大区 ID      |
| regionName   | string | 大区名称         |
| officeId     | long   | 所属办事处 ID    |
| officeName   | string | 办事处名称       |
| districtId   | long   | 所属片区 ID      |
| districtName | string | 片区名称         |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "townshipId": 440305001,
    "townshipName": "南头街道",
    "regionId": 1,
    "regionName": "华南大区",
    "officeId": 101,
    "officeName": "深圳办事处",
    "districtId": 1001,
    "districtName": "南山片区"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

**说明**：若某街道未关联片区，则 `data` 返回 `null`。

------

#### 获取所有省份列表

接口描述：获取全国所有省份/直辖市/自治区列表，无需传入参数

请求：

```
GET /api/area/province-list
```

请求参数：无

返回参数：

| 字段         | 类型   | 说明           |
| ------------ | ------ | -------------- |
| adcode       | string | 当前区划代码   |
| name         | string | 名称           |
| parentAdcode | string | 上级区划代码   |
| parentName   | string | 上级区划名称   |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "adcode": "110000", "name": "北京市", "parentAdcode": "000000", "parentName": "中国" },
    { "adcode": "440000", "name": "广东省", "parentAdcode": "000000", "parentName": "中国" }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 获取所有城市列表

接口描述：获取全国所有地级市/直辖市列表，无需传入参数

请求：

```
GET /api/area/city-list
```

请求参数：无

返回参数：同上（adcode / name / parentAdcode / parentName）

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "adcode": "440300", "name": "深圳市", "parentAdcode": "440000", "parentName": "广东省" },
    { "adcode": "440100", "name": "广州市", "parentAdcode": "440000", "parentName": "广东省" }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 获取所有区县列表

接口描述：获取全国所有市辖区/县/县级市列表，无需传入参数

请求：

```
GET /api/area/county-list
```

请求参数：无

返回参数：同上（adcode / name / parentAdcode / parentName）

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "adcode": "440305", "name": "南山区", "parentAdcode": "440300", "parentName": "深圳市" },
    { "adcode": "440306", "name": "宝安区", "parentAdcode": "440300", "parentName": "深圳市" }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

#### 获取所有乡镇/街道列表

接口描述：获取全国所有乡镇和街道列表，无需传入参数

请求：

```
GET /api/area/township-list
```

请求参数：无

返回参数：同上（adcode / name / parentAdcode / parentName）

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    { "adcode": "440305001", "name": "南头街道", "parentAdcode": "440305", "parentName": "南山区" },
    { "adcode": "440305002", "name": "南山街道", "parentAdcode": "440305", "parentName": "南山区" }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```
