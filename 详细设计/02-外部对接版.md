## 一、总体表设计

> **接口鉴权说明**：本系统接口按用途分为两类：
> - **后台管理接口** `/api/admin/**`：需 JWT Token 登录认证，无需 API 签名
> - **对外开放接口** `/api/open/**`：需 MD5 签名验证（appKey + timestamp + nonce + body + secret），无需 JWT Token
>
> 本文档所列查询接口均属于对外开放接口，路径前缀为 `/api/open/`，需携带签名请求头。

```
area（行政区）
        ↑
area_relation（行政区与组织架构映射关系表）
        ↑
organization（销售组织架构表）
```

------

## 二、完整表定义

### 1. 行政区表（area）

```sql
CREATE TABLE `area` (
  `id` int(10) unsigned NOT NULL COMMENT '主键 ID',
  `pid` int(10) unsigned NOT NULL DEFAULT 0 COMMENT '父级 ID',
  `level` int(10) unsigned NOT NULL DEFAULT 0 COMMENT '层级：0-省 1-市 1-区 2-县 3-乡/街道',
  `name` varchar(240) NOT NULL DEFAULT '' COMMENT '名称',
  `short_name` varchar(240) NOT NULL DEFAULT '' COMMENT '简称',
  `full_name` varchar(240) NOT NULL DEFAULT '' COMMENT '全路径名称',
  `pin_yin` varchar(240) NOT NULL DEFAULT '' COMMENT '拼音',
  `city_code` varchar(10) NOT NULL DEFAULT '' COMMENT '城市编码',
  `zip_code` varchar(10) NOT NULL DEFAULT '' COMMENT '邮编',
  `lng` decimal(10,7) unsigned NOT NULL DEFAULT 0.0000000 COMMENT '经度',
  `lat` decimal(10,7) unsigned NOT NULL DEFAULT 0.0000000 COMMENT '纬度',
  `sort_index` int(10) unsigned NOT NULL DEFAULT 0 COMMENT '排序',
  `path` varchar(500) NOT NULL DEFAULT '' COMMENT '路径，如 1/2/3',
  `version` varchar(64) NOT NULL DEFAULT '' COMMENT '版本号',
  `del_flag` tinyint(1) NOT NULL DEFAULT 0 COMMENT '删除标志',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_pid` (`pid`),
  KEY `idx_path` (`path`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='行政区表';
```

### 2. 统一组织表（organization）

```sql
CREATE TABLE `organization` (
  `id` int(10) NOT NULL AUTO_INCREMENT COMMENT '主键 ID',
  `name` varchar(50) NOT NULL DEFAULT '' COMMENT '组织名称',

  `type` tinyint(2) NOT NULL DEFAULT 0 COMMENT '组织类型 1-大区 2-办事处 3-片区',

  `parent_id` int(10) NOT NULL DEFAULT 0 COMMENT '父级组织 ID',
  `level` int(5) NOT NULL DEFAULT 0 COMMENT '层级',

  `path` varchar(500) NOT NULL DEFAULT '' COMMENT '组织路径，如 1/10/100',
  `sort_index` int(10) NOT NULL DEFAULT 0 COMMENT '排序',

  `leader_id` int(10) NOT NULL DEFAULT 0 COMMENT '负责人 ID（预留扩展）',

  `del_flag` tinyint(1) NOT NULL DEFAULT 0 COMMENT '删除标志',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_type` (`type`),
  KEY `idx_path` (`path`),
  KEY `idx_parent_type` (`parent_id`, `type`)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='组织架构表（大区/办事处/片区统一）';
```

### 3. 行政区与组织映射表（area_relation）

```sql
CREATE TABLE `area_relation` (
  `id` int(11) NOT NULL AUTO_INCREMENT COMMENT '主键 ID',

  `area_id` int(11) NOT NULL DEFAULT 0 COMMENT '行政区 ID',
  `org_id` int(11) NOT NULL DEFAULT 0 COMMENT '组织 ID',

  `del_flag` tinyint(1) NOT NULL DEFAULT 0 COMMENT '删除标志',

  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

  PRIMARY KEY (`id`),

  UNIQUE KEY `uk_area_org` (`area_id`, `org_id`),

  KEY `idx_area_id` (`area_id`),
  KEY `idx_org_id` (`org_id`)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='行政区与组织映射关系表';
```

------

## 三、数据示例

组织树示例，organization 表数据：

```
id | name      | type | parent_id
----------------------------------
1  | 华南大区   | 1    | 0
2  | 深圳办事处 | 2    | 1
3  | 南山片区   | 3    | 2
```

形成的层级关系：

```
华南大区
   └── 深圳办事处
          └── 南山片区
```

------

## 四、查询场景

### 1. 查某行政区属于哪个片区

```sql
SELECT o.*
FROM area_relation ar
JOIN organization o ON ar.org_id = o.id
WHERE ar.area_id = ?
AND o.type = 3;
```

### 2. 查某片区属于哪个大区（向上查）

```sql
SELECT parent.*
FROM organization child
JOIN organization parent ON child.path LIKE CONCAT(parent.path, '%')
WHERE child.id = ?
AND parent.type = 1;
```

利用 path 字段避免递归查询。

### 3. 查某大区下所有片区

```sql
SELECT *
FROM organization
WHERE path LIKE '1/%'
AND type = 3;
```


## 五、 开放查询接口

### 查询行政区所属组织

接口描述：根据行政区 ID 查询其所属的组织（片区/办事处/大区）

请求：

```
GET /api/open/geo/org
```

请求参数：

| 参数   | 类型 | 必填 | 说明       |
| ------ | ---- | ---- | ---------- |
| areaId | long | 是   | 行政区 ID  |

返回参数：

| 字段       | 类型   | 说明       |
| ---------- | ------ | ---------- |
| areaId     | long   | 行政区 ID  |
| areaName   | string | 行政区名称 |
| orgId      | long   | 片区 ID    |
| orgName    | string | 片区名称   |
| officeId   | long   | 办事处 ID  |
| officeName | string | 办事处名称 |
| regionId   | long   | 大区 ID    |
| regionName | string | 大区名称   |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "areaId": 10001,
    "areaName": "南头街道",
    "orgId": 1001,
    "orgName": "南山片区",
    "officeId": 101,
    "officeName": "深圳办事处",
    "regionId": 1,
    "regionName": "华南大区"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

### 查询片区所属上级

接口描述：查询片区的直接上级（办事处）及所属大区

请求：

```
GET /api/admin/district/parent
```

请求参数：

| 参数   | 类型 | 必填 | 说明   |
| ------ | ---- | ---- | ------ |
| areaId | long | 是   | 片区 ID |

返回参数：

| 字段       | 类型   | 说明       |
| ---------- | ------ | ---------- |
| areaId     | long   | 片区 ID    |
| areaName   | string | 片区名称   |
| officeId   | long   | 办事处 ID  |
| officeName | string | 办事处名称 |
| regionId   | long   | 大区 ID    |
| regionName | string | 大区名称   |


### 查询办事处所属大区

接口描述：查询办事处的所属大区

请求：

```
GET /api/admin/office/region
```

请求参数：

| 参数     | 类型 | 必填 | 说明     |
| -------- | ---- | ---- | -------- |
| officeId | long | 是   | 办事处 ID |

返回参数：

| 字段       | 类型   | 说明       |
| ---------- | ------ | ---------- |
| officeId   | long   | 办事处 ID  |
| officeName | string | 办事处名称 |
| regionId   | long   | 大区 ID    |
| regionName | string | 大区名称   |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "officeId": 101,
    "officeName": "深圳办事处",
    "regionId": 1,
    "regionName": "华南大区"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```



### 组织树查询

接口描述：获取完整的组织架构树（大区 → 办事处 → 片区）

请求：

```
GET /api/admin/org/tree
```

请求参数：

| 参数     | 类型 | 必填 | 说明                   |
| -------- | ---- | ---- | ---------------------- |
| regionId | long | 否   | 大区 ID，不传返回全部   |
| status   | int  | 否   | 状态筛选               |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "华南大区",
      "type": 1,
      "children": [
        {
          "id": 101,
          "name": "深圳办事处",
          "type": 2,
          "children": [
            {
              "id": 1001,
              "name": "南山片区",
              "type": 3,
              "children": []
            }
          ]
        }
      ]
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

### 省列表查询

接口描述：获取所有省份列表

请求：

```
GET /api/open/area/provinces
```

请求参数：无

返回参数：

| 字段      | 类型   | 说明         |
| --------- | ------ | ------------ |
| id        | long   | 省份 ID      |
| name      | string | 省份名称     |
| shortName | string | 简称         |
| cityCode  | string | 城市编码     |
| lng       | double | 经度         |
| lat       | double | 纬度         |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 100000,
      "name": "广东省",
      "shortName": "广东",
      "cityCode": "440000",
      "lng": 113.280637,
      "lat": 23.125178
    },
    {
      "id": 110000,
      "name": "北京市",
      "shortName": "北京",
      "cityCode": "110000",
      "lng": 116.407526,
      "lat": 39.90403
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

### 市列表查询

接口描述：根据省份 ID 查询下属所有城市

请求：

```
GET /api/open/area/cities
```

请求参数：

| 参数      | 类型 | 必填 | 说明     |
| --------- | ---- | ---- | -------- |
| provinceId | long | 是   | 省份 ID  |

返回参数：

| 字段      | 类型   | 说明         |
| --------- | ------ | ------------ |
| id        | long   | 城市 ID      |
| name      | string | 城市名称     |
| shortName | string | 简称         |
| cityCode  | string | 城市编码     |
| lng       | double | 经度         |
| lat       | double | 纬度         |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 440300,
      "name": "深圳市",
      "shortName": "深圳",
      "cityCode": "440300",
      "lng": 114.057868,
      "lat": 22.543099
    },
    {
      "id": 440100,
      "name": "广州市",
      "shortName": "广州",
      "cityCode": "440100",
      "lng": 113.280637,
      "lat": 23.125178
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

### 区/县列表查询

接口描述：根据城市 ID 查询下属所有区县

请求：

```
GET /api/open/area/counties
```

请求参数：

| 参数     | 类型 | 必填 | 说明     |
| -------- | ---- | ---- | -------- |
| cityId   | long | 是   | 城市 ID  |

返回参数：

| 字段      | 类型   | 说明         |
| --------- | ------ | ------------ |
| id        | long   | 区县 ID      |
| name      | string | 区县名称     |
| shortName | string | 简称         |
| level     | int    | 类型：1-市辖区 2-县 3-县级市 |
| cityCode  | string | 城市编码     |
| lng       | double | 经度         |
| lat       | double | 纬度         |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 440305,
      "name": "南山区",
      "shortName": "南山",
      "level": 1,
      "cityCode": "440305",
      "lng": 113.928433,
      "lat": 22.489046
    },
    {
      "id": 440306,
      "name": "宝安区",
      "shortName": "宝安",
      "level": 1,
      "cityCode": "440306",
      "lng": 113.884837,
      "lat": 22.652203
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

### 乡镇/街道列表查询

接口描述：根据区县 ID 查询下属所有乡镇和街道

请求：

```
GET /api/open/area/townships
```

请求参数：

| 参数     | 类型 | 必填 | 说明       |
| -------- | ---- | ---- | ---------- |
| countyId | long | 是   | 区县 ID    |

返回参数：

| 字段      | 类型   | 说明             |
| --------- | ------ | ---------------- |
| id        | long   | 乡镇/街道 ID     |
| name      | string | 名称             |
| shortName | string | 简称             |
| type      | string | 类型：街道/镇/乡 |
| cityCode  | string | 城市编码         |
| lng       | double | 经度             |
| lat       | double | 纬度             |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 440305001,
      "name": "南头街道",
      "shortName": "南头",
      "type": "街道",
      "cityCode": "440305001",
      "lng": 113.942695,
      "lat": 22.532848
    },
    {
      "id": 440305002,
      "name": "南山街道",
      "shortName": "南山",
      "type": "街道",
      "cityCode": "440305002",
      "lng": 113.915856,
      "lat": 22.528993
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

### 行政区详情查询

接口描述：根据行政区 ID 查询详细信息（含完整上级路径）

请求：

```
GET /api/open/area/detail
```

请求参数：

| 参数   | 类型 | 必填 | 说明         |
| ------ | ---- | ---- | ------------ |
| areaId | long | 是   | 行政区 ID    |

返回参数：

| 字段       | 类型   | 说明           |
| ---------- | ------ | -------------- |
| id         | long   | 行政区 ID      |
| name       | string | 名称           |
| fullName   | string | 完整路径名称   |
| level      | int    | 层级           |
| provinceId | long   | 所属省份 ID    |
| provinceName | string | 省份名称   |
| cityId     | long   | 所属城市 ID    |
| cityName   | string | 城市名称       |
| countyId   | long   | 所属区县 ID    |
| countyName | string | 区县名称       |
| townshipId | long   | 所属乡镇/街道 ID |
| townshipName | string | 乡镇/街道名称 |
| lng        | double | 经度           |
| lat        | double | 纬度           |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 440305001,
    "name": "南头街道",
    "fullName": "广东省深圳市南山区南头街道",
    "level": 3,
    "provinceId": 100000,
    "provinceName": "广东省",
    "cityId": 440300,
    "cityName": "深圳市",
    "countyId": 440305,
    "countyName": "南山区",
    "townshipId": 440305001,
    "townshipName": "南头街道",
    "lng": 113.942695,
    "lat": 22.532848
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

### 行政区树查询

接口描述：根据类型获取行政区树（可指定层级深度）

请求：

```
GET /api/open/area/tree
```

请求参数：

| 参数    | 类型 | 必填 | 说明                                         |
| ------- | ---- | ---- | -------------------------------------------- |
| type    | int  | 否   | 起始层级：0-省（默认）1-市 2-区县 3-乡镇街道 |
| areaId  | long | 否   | 起始行政区 ID（type 为 0 时不传）            |
| depth   | int  | 否   | 向下查询层级深度，默认 1                     |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": 100000,
      "name": "广东省",
      "children": [
        {
          "id": 440300,
          "name": "深圳市",
          "children": [
            {
              "id": 440305,
              "name": "南山区",
              "children": []
            }
          ]
        }
      ]
    }
  ],
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

------

### 根据街道行政区 ID 查询所属片区

接口描述：根据街道（乡镇）行政区 ID 查询其所属片区信息，返回结果包含大区、办事处、片区各自的 ID 和名称

请求：

```
GET /api/open/geo/org/by-township
```

请求参数：

| 参数       | 类型 | 必填 | 说明               |
| ---------- | ---- | ---- | ------------------ |
| townshipId | long | 是   | 街道/乡镇行政区 ID |

返回参数：

| 字段         | 类型   | 说明             |
| ------------ | ------ | ---------------- |
| townshipId   | long   | 街道/乡镇行政区 ID |
| townshipName | string | 街道/乡镇名称    |
| regionId     | long   | 所属大区 ID      |
| regionName   | string | 大区名称         |
| officeId     | long   | 所属办事处 ID    |
| officeName   | string | 办事处名称       |
| districtId   | long   | 所属片区 ID      |
| districtName | string | 片区名称         |

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "townshipId": 440305001,
    "townshipName": "南头街道",
    "regionId": 1,
    "regionName": "华南大区",
    "officeId": 101,
    "officeName": "深圳办事处",
    "districtId": 1001,
    "districtName": "南山片区"
  },
  "timestamp": 1713580800000,
  "traceId": "a1b2c3d4e5f6"
}
```

**说明**：若某街道未关联片区，则 `data` 返回 `null`。

------

## 六、快速测试

https://aecpooql8k.apifox.cn
访问密码：OYf8EhbD